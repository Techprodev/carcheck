export default class User {
  constructor(username, mobile, email, password, roleType) {
    this.username = username;
    this.mobile = mobile;
    this.email = email;
    this.password = password;
    this.roleType = roleType;
  }
}
